name: Build Vue
on: 
  push:
    branches: [ "production" ]
  pull_request:
    branches: [ "production" ]
jobs:
  build_vue:
    runs-on: ubuntu-latest
    name: Build Vue
    steps:
    - uses: actions/checkout@v2
    - name: Run a one-line script
      run: sh ./deploy.sh
    
